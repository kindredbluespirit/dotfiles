;;;;  Managed by chemacs now, this file is obsolete

;; (setq xdg-config-home (getenv "XDG_CONFIG_HOME"))
;; (if (not (string= user-emacs-directory (concat xdg-config-home "/emacs/"))) 
;;   (save-buffers-kill-terminal))

(provide '01-xdg-core)